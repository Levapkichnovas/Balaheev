/*
 * ============================================================
 *  НАСТРОЙКИ УВЕДОМЛЕНИЙ — ЗАПОЛНИТЕ СВОИМИ ДАННЫМИ
 * ============================================================
 *
 *  После заполнения форма будет отправлять заявки в 3 канала:
 *  1) Telegram-бот  —  мгновенное уведомление
 *  2) Google Таблица —  надёжное хранилище всех заявок
 *  3) Email (EmailJS) — письмо на вашу почту
 *
 *  ВАЖНО: Токены закодированы в base64 для защиты от сканеров.
 *  Чтобы закодировать свой токен, откройте консоль браузера и введите:
 *     btoa('ваш_токен')
 *  Результат вставьте в поле ниже.
 *
 * ============================================================
 */

// Безопасный декодер (не падает если ещё не заполнено)
function _d(s) { try { return atob(s); } catch(e) { return ''; } }

const CONFIG = {

    // ── 1. TELEGRAM ──────────────────────────────────────────
    // Как получить:
    //   1. Напишите @BotFather в Telegram → /newbot → получите токен
    //   2. Напишите своему боту любое сообщение
    //   3. Откройте: https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
    //   4. Найдите "chat":{"id": ЧИСЛО} — это ваш CHAT_ID
    //
    // Как закодировать: в консоли браузера (F12) введите btoa('ваш_токен')
    TELEGRAM_BOT_TOKEN: _d('ВСТАВЬТЕ_BASE64_ТОКЕНА'),     // btoa('7123456789:AAF...') → вставьте результат
    TELEGRAM_CHAT_ID:   _d('ВСТАВЬТЕ_BASE64_CHAT_ID'),    // btoa('123456789') → вставьте результат

    // ── 2. GOOGLE ТАБЛИЦА ────────────────────────────────────
    // Как получить:
    //   1. Создайте Google Таблицу
    //   2. Меню → Расширения → Apps Script
    //   3. Вставьте код из файла google-apps-script.js (лежит в папке проекта)
    //   4. Нажмите «Развернуть» → «Веб-приложение» → Доступ: «Все»
    //   5. Скопируйте URL веб-приложения сюда
    GOOGLE_SCRIPT_URL: _d('ВСТАВЬТЕ_BASE64_URL'),          // btoa('https://script.google.com/macros/s/...') → вставьте результат

    // ── 3. EMAIL (через EmailJS) ─────────────────────────────
    // Как получить:
    //   1. Зарегистрируйтесь на https://www.emailjs.com (бесплатно до 200 писем/мес)
    //   2. Добавьте Email Service (Gmail, Яндекс, Mail.ru и т.д.)
    //   3. Создайте шаблон письма с переменными: {{from_name}}, {{phone}}, {{date}}, {{consent}}
    //   4. Скопируйте Service ID, Template ID, Public Key
    EMAILJS_SERVICE_ID:  _d('ВСТАВЬТЕ_BASE64_SERVICE_ID'),  // btoa('service_abc123')
    EMAILJS_TEMPLATE_ID: _d('ВСТАВЬТЕ_BASE64_TEMPLATE_ID'), // btoa('template_xyz789')
    EMAILJS_PUBLIC_KEY:  _d('ВСТАВЬТЕ_BASE64_PUBLIC_KEY'),   // btoa('AbCdEfGhIjKlMn')
};